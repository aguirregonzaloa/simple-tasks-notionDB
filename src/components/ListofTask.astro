---
import { getCollection } from "astro:content";

const posts = await getCollection("database");
// export const prerender = true;
---

<section id="list-tasks">
  <ul>
    {
      posts.map((item) => (
        <article>
          <h3 class="text-2xl font-semibold text-blue-900">
            {item.data.properties["Tarea"]}
          </h3>
          <p class="text-blue-800">{item.data.properties["Estado"]}</p>
          <p class="text-blue-800">{item.data.properties["Descripción"]}</p>
        </article>
      ))
    }
  </ul>
</section>
